# Common-base practices audit

- **Repo:** `third-party-risk-ddq`
- **Catalog id:** `third-party-risk-ddq` (package `tprm_ddq`, env prefix `TPRM_DDQ`)
- **Catalogue reference:** [`common-base-practices.md`](https://github.com/portable-genai/.github/blob/main/common-base-practices.md) (checks A1..G7)

This file ships PRE-FILLED with the verdicts the template can honestly claim on the day the repo
is rendered, so a new repo never enters the audit matrix owing an unwritten audit. It is not a
claim that the repo was inspected: re-run each check against the tree and correct the row before
citing this as evidence. The rows marked GAP are the day-one work list, in priority order.

Applicability: this repo ships an `agent/` surface (tool functions plus the A2A card served at
`/.well-known/agent-card.json`), a `ui/` micro-frontend, a full `scripts/` demo surface and a
complete `infra/terraform/` stack, so the `[ui]`, `[demo]` and `[infra]` checks all apply. The
Terraform is written, validated and tested against a mocked provider; it has never been applied,
which is a deployment fact rather than an audit gap, and the serving edge is additionally
refused while `src/tprm_ddq/managed_readiness.py` still lists construction-only managed
adapters. The agent is scaffolded and tested but not yet registered with `agent-registry`; see the R4 row in
`COMPLIANCE.md`. A repo with no user-facing surface should run `make drop-ui` and mark the
`[ui]` rows N/A, rather than leaving a UI half-wired.
**Load-bearing** checks (a FAIL breaks a shared catalog guarantee) are A1-A6, C1-C5, D1-D3 and E1.

| Check | Verdict | Evidence / gap |
|---|---|---|
| **A1** Hexagonal core, stdlib-only domain `[all]` **(load-bearing)** | PASS | `domain/` imports only stdlib plus the stdlib-only commons; no web framework, cloud SDK or HTTP client. |
| **A2** Ports are `@runtime_checkable` Protocols, re-exported once `[all]` **(load-bearing)** | PASS | `ports/__init__.py` re-exports every port and the `PORT_PROTOCOLS` map; the container asserts conformance as it binds; `tests/contract/test_port_parity.py` also checks every Protocol member is declared on each adapter CLASS. |
| **A3** Runtime/data profiles are explicitly swappable `[all]` **(load-bearing)** | PASS | `TPRM_DDQ_PROFILE = local\|gcp\|onprem`; the per-port `adapters:` block in `config/settings.yaml` is the binding map; the offline gate installs no cloud SDK. |
| **A4** One adapter constructor `Adapter(settings)` `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py::test_adapter_constructs_with_a_single_settings_argument` covers every port in every profile. |
| **A5** Lazy cloud imports in cloud adapters `[all]` **(load-bearing)** | PASS | The `google.cloud` import lives inside `CloudAuditAdapter.record`. Proved by BLOCKING the import in a fresh interpreter (`tests/contract/_sdk_free_probe.py`), not by the SDK happening to be absent from the machine. |
| **A6** Contract tests enforce the hexagon; port map cannot drift `[all]` **(load-bearing)** | PASS | `tests/contract/test_port_parity.py` asserts set equality across ALL FIVE homes of a port (Protocol map, `DEFAULT_BINDINGS`, `Container` accessor, settings file, canonical-call table), so an unregistered port cannot run untested; `tests/contract/test_behavioral_parity.py` proves the offline family answers, the on-premises family raises and the managed family refuses rather than silently succeeding; `tests/unit/test_settings_file.py` holds the two binding tables equal. |
| **A7** Kernel vs vertical split in the domain `[all]` | PASS | `domain/kernel.py` (neutral) vs `domain/models.py` (this vertical); `models.py` imports `kernel`, never the reverse. |
| **A8** Consume platform horizontals via thin delegates `[all]` | PARTIAL | `human-review-console` is consumed through `adapters/*/review_router.py` via the shared `review-kit`. Wire the remaining horizontals (guardrail, KB, observability, quality) as this vertical needs them; `COMPLIANCE.md` carries an explicit TODO row for each. |
| **B1** Consequential math is deterministic, pure, replayable `[agentic]` | PASS | `domain/triage_service.py` is pure stdlib and replayable; an LLM narrates only, and never produces the severity band. |
| **B2** Every claim carries a citation; empty retrieval is a hard error `[agentic]` | PASS | Every `TriageResult` carries a `Citation`. Extend to a hard error on empty retrieval when a retrieval port is added. |
| **B3** Maker-checker on every consequential output `[agentic]` | PASS | `requires_human_review` plus rule R8 routing through `ReviewRouterPort`, in the API request and the CLI; `tests/unit/test_review_routing.py` proves an escalation produces an outbound review and a non-escalation does not, on the API, CLI and agent paths alike. |
| **B4** Bank-owned policy numbers in config, defaults = reference `[all]` | GAP | The severity keyword bands are module constants in `triage_service.py`. Lift them into a frozen policy dataclass with a `from_policy(...)` constructor and a `policy:` block in `config/settings.yaml`. |
| **B5** Open taxonomy: `StrEnum` vocabularies, engines typed on `str` `[all]` | PASS | `Severity` and `Decision` are `LenientStrEnum` from the commons, so an unknown value from a future release does not crash the reader. |
| **C1** Identity resolved server-side; client actor/ACL discarded `[all]` **(load-bearing)** | PASS | `TriageRequest` has no `actor` field; the audit actor and the review maker are both the verified `Principal`. The managed adapter EARNS the word verified: `id_token.verify_token` is called with the configured `TPRM_DDQ_IAP_AUDIENCE` (three-state; unset or emptied refuses rather than verifying without an audience, which google-auth documents as not verifying it at all) and with IAP's own key set rather than google-auth's OAuth2 default, the issuer is checked here because `verify_token` does not check it, and both the call and the lazy import are wrapped so no caller-supplied header can answer 500. `tests/unit/test_iap_identity.py` runs in this gate; `tests/unit/test_iap_crypto_matrix.py` runs the real verifier over a locally minted key in the `iap-verifier` job, which fails if it skips. **The claim half is the commons decision too (2026-08-26).** `resolve()` no longer constructs a `Principal` at all: everything after the signature check is one `hex_service_kit.federation.principal_from_iap_claims(claims, _FEDERATION_POLICY, source="gcp-iap", include_subject_principal=False)` call, and the cryptography stays in the adapter because the kit's core is pure stdlib with zero runtime dependencies and verifies nothing. `_FEDERATION_POLICY` is a reviewed literal carrying `tenant_from_hosted_domain=True`: this deployment configures no domain map and IAP already restricts the audience to one organisation, so the `hd` claim IS the tenant id here, and that is stated as an OPT-IN rather than left to a fallback. `include_subject_principal` is stated for the same reason: this adapter family leaves the tuple to the group map alone while the `adapters/gcp/iap_identity.py` family grants `user:<subject>` and the difference is an authorization decision, not a formatting one. `tests/unit/test_iap_claim_half.py` guards it, and it was RED first, offline, in three stages. (1) Against the adapter exactly as it shipped the module would not even import: `ImportError: cannot import name '_FEDERATION_POLICY'`, which is the state in which a load-bearing tenant can be emptied by a default nobody wrote down. (2) With only the policy object added and the hand-rolled tail left standing it scored 4 failed / 6 passed, and those four ARE the finding: the `acr` row and the mixed-case `hd` row of the comparison disagreed, and both `test_a_claim_set_from_another_issuer_cannot_become_a_principal` and `test_a_verified_assertion_that_names_nobody_is_refused` reported `DID NOT RAISE`, because with the claim-set precheck removed the hand-rolled tail read `claims["email"]` and built a verified identity out of a token issued by somebody else. (3) After adoption, 10 passed. Three deliberate defects on the fixed tree keep the guard honest: flipping `include_subject_principal` scores 6 failed / 4 passed, clearing `tenant_from_hosted_domain` fails the passthrough guard, and passing a fresh `FederationPolicy()` at the call site instead of the reviewed one scores 4 failed / 6 passed, on the resolved tenant rather than on an attribute. **Two behaviour changes are recorded rather than normalised away.** `assurance` now prefers an `acr` claim where the shipped tail hardcoded `"iap"`; nothing in this repository reads `Principal.assurance` for an authorization decision, so the change is audit-visible only, and IAP assertions do not carry `acr` in practice. The tenant is now lower-cased where the shipped tail passed `hd` through with only `.strip()`; `principal.tenant` is a partition key, so a mixed-case hosted domain would change the partition, and Google normalises `hd` to lower case in practice. Both were found by EXECUTING the commons against this adapter over the same claim sets, not by reading it. |
| **C2** Object-level authz derived server-side; tenant isolation by data tags `[all]` **(load-bearing)** | N/A (day one) | No queryable data store yet. The tenant partition is carried on outbound reviews; add ACL matchers with the first store. |
| **C3** Redact before everything `[agentic]` **(load-bearing)** | PASS | Enforced by CONSTRUCTION, not per call site: `AuditEvent.__post_init__` masks the summary and every citation field (`domain/kernel.py`), so neither audit writer (`TriageService._triage`, `AssessmentService._record`) nor any surface added later can write a raw identifier to the WORM record by forgetting. `adapters/_review_payload.py` redacts every citation field AND the derived key fields (`case_ref`, `source_key`) before the wire, against EVERY jurisdiction's rows because the console is a shared sink: the subject is masked once and reused, so a structural-looking field cannot republish what the narrative fields removed. `tests/unit/test_audit_redaction.py` drives the real paths, reads the record back, and scans the SERIALISED review payload so a field added later is covered by default. The vendor REGISTER is the audit record's sibling sink and was the one this check missed: `AssessmentService.assess` hands the same citation tuple to the audit writer and to `RegisterEntry`, and only the first masked it, so an extraction citation's `snippet` (`line[:80]` of an uploaded document) reached a long-lived tenant-scoped store that `operational-resilience-mapping` reads over A2A. Closed the same way and in the same place: `RegisterEntry.__post_init__` masks every citation field, with `vendor` and `tenant` left intact because they are the row identity the store authorises on. `tests/unit/test_register_redaction.py` drives the document-to-register path end to end and was observed RED against the unfixed row. |
| **C4** Jurisdiction-driven PII packs keep the gate honest `[agentic]` **(load-bearing)** | PASS | `domain/pii.py` selects and ORDERS rows from the shared `pii-kit`; `tests/unit/test_not_falsely_green.py` proves the safety metric can go red. |
| **C5** Fail-closed defaults everywhere `[all]` **(load-bearing)** | PASS | Module-scope loopback exposure guard whose posture is derived from the identity BINDING (the adapter declares whether it verifies an end user; silence reads as client-asserted) and never from a service credential, so it covers the unconsented posture, the deliberate seeded-persona one and the unimplemented on-premises one alike, with or without an S2S token set; the profile resolves at IMPORT into a `ProfileChoice` where unset is NO CHOICE and an emptied or unknown value refuses to boot, with relaxations keyed off `exposure_profile` and the loopback bound off `bind_profile`; `tests/unit/test_profile_single_source.py` fails the build if a second module re-derives the profile and `tests/unit/test_three_state_env_reads.py` fails it for any two-state environment read that ships; three-state settings reads; CORS that never becomes `*`; a managed review router that refuses rather than swallowing an escalation. |
| **C6** Security-header baseline on every surface `[ui]` | PASS | `add_security_headers` on the API app; `ui/proxy.ts` applies the same baseline to EVERY UI response (including error responses) from `ui/lib/embed-policy.mjs`, the single policy module the API route also uses for CORS. `frame-ancestors` and the tenant CORS list are allowlists that refuse a wildcard however it is written; `ui/tests/embed-policy.test.mjs` and `tests/unit/test_ui_surface.py` are the standing gates. |
| **C7** S2S calls authenticated, https-only outside loopback `[all]` | PASS | Inbound via `make_require_service_caller`; outbound via the review kit, which refuses a plaintext non-loopback URL and a missing bearer at construction. |
| **C8** Web login flow hardening `[ui]` | N/A (delegated) | This UI owns no login flow by design: under `secure` the platform in front of it authenticates and injects a signed assertion, which the UI FORWARDS without parsing or trusting a parsed copy, and a request with no assertion is refused with 401 rather than downgraded. `onprem` raises instead of falling back to the seeded personas, and an UNSET `UI_PROFILE` raises too rather than selecting the persona profile. Revisit if this repo ever authenticates users itself. |
| **C9** Tamper-evident audit with honest limits `[all]` | PASS | Append-only and hash-chained, AND externally anchored: `audit_anchor_path` (`TPRM_DDQ_AUDIT_ANCHOR`) is wired from settings through to the commons log, so a truncated but internally consistent tail is detectable. `tests/unit/test_audit_anchor.py` proves the detection, proves the control case goes UNDETECTED without an anchor, and proves an append after truncation refuses rather than re-anchoring. Operating rules in `docs/runbook.md`. |
| **C10** No secret values in the repo `[all]` | PASS | `config/settings.yaml` holds env var NAMES and non-secret defaults only; `.env.secrets.example` holds placeholders; `.gitignore` excludes the real files. |
| **D1** Locked, reproducible installs everywhere `[all]` **(load-bearing)** | PASS | `requirements-dev.lock` and `requirements-gcp.lock` are committed; `make install`, CI and the Dockerfile all install from a lock then the project with `--no-deps`; `ruff` is pinned exactly. |
| **D2** Digest-pinned images, SHA-pinned Actions, dependabot, CI audit `[all]` **(load-bearing)** | PASS | `FROM python:3.12-slim@sha256:...`; every action in the shared workflow is SHA-pinned; `dependabot.yml` covers pip (daily), docker, github-actions and npm (daily, for `ui/`); the supply-chain job runs `pip-audit` over both locks and the ui-gate job runs `npm audit --audit-level=high`, both as HARD failures. `ui/package-lock.json` is committed and the workflow installs with `npm ci`. **Which of those actually executes is worth stating rather than implying:** GitHub Actions became the fleet's live CI on 2026-09-02, so the workflow above is no longer a reviewed contract Cloud Build is digest-bound to but the RENDERED caller that actually runs on a push. The hosted signal since 2026-09-02 is GitHub Actions, which runs this repository's `gate` target on every push to `main` and on every pull request, with that PR check required before a merge. The audits above therefore run whenever `gate` runs; a scheduled dependency audit independent of a push is a contract this repository does not yet have. |
| **D3** Whole gate runs offline, zero org secrets `[all]` **(load-bearing)** | PASS | `make gate` needs no network, no cloud SDK and no credentials; the workflow references no `secrets.`. Anything that does need a live service lives in `tests/integration/`, and `tests/unit/test_test_layout.py` fails the build if such a module is not marked. |
| **D4** Non-root, minimal, healthchecked container `[infra]` | PASS | Multi-stage build, `USER appuser` (uid 10001), `HEALTHCHECK` on `/healthz`, no build tools in the runtime stage, `.dockerignore` keeps tests, docs and env files out. |
| **D5** Deploy-time residency/sovereignty, parameterised `[infra]` | PASS | `infra/terraform/` carries the stack: the residency allowlist validated at plan (`variables.tf`), the `gcp.resourceLocations` Org Policy plus the no-service-account-keys and uniform-bucket-access constraints (`org_policy.tf`), a regional CMEK key with a per-service-agent binding (`kms.tf`), a dry-run-first VPC-SC perimeter (`vpc_sc.tf`), the locked WORM log bucket with DATA_READ audit logging (`logging_worm.tf`), the alert policies (`monitoring.tf`) and the load-balancer-only serving edge with IAP and Cloud Armor (`production_edge.tf`). `production_edge.tftest.hcl` runs fourteen refusals against a mocked provider (`terraform init -backend=false && terraform test`), so residency, retention, image-digest and rule-R8 preconditions are proved with no project and no credentials, and `managed_readiness.tf` refuses to authorise the serving edge at all while the managed adapters are placeholders. Not yet applied anywhere, and the private-egress rule and Interconnect attachment remain adopter network decisions (`COMPLIANCE.md` P-01). |
| **E1** Offline eval smoke guards merge; `model-quality-gate` owns promotion `[agentic]` **(load-bearing)** | PASS | `eval/run_eval.py --mode smoke\|gate`; smoke runs in the gate, gate mode routes to `model-quality-gate` and refuses to run off the managed profile. |
| **E2** Safety metric with strictest threshold, no false green `[agentic]` | PASS | `pii_safety >= 0.99` is scored two ways (the shared pack scan plus an independent planted-literal oracle) over every CONTENT field of the audit records the REAL services wrote, citations included, rather than a string the eval builds itself out of the dataset (which would score the redactor against itself, over text the service never produced). `test_not_falsely_green.py` falsifies the SHIPPED `eval.run_eval.pii_safety`, not a local re-implementation of it. |
| **E3** Fixtures and golden data obviously fictional `[all]` | PASS | The golden set uses obviously fictional parties and `.example` domains. |
| **F1** Demo is code, offline, one command, presenter-paced `[all]` | PASS | `make demo` is one command: `scripts/walkthrough.py` starts its own loopback server, narrates each of the eight steps on the terminal (never on the page) and waits for the presenter. Everything runs offline with the stdlib alone, driving the REAL services; `scripts/render_ui.py` renders the same audit-first panels to static HTML for screenshots. |
| **F2** Demo cannot rot silently `[all]` | PASS | `tests/unit/test_demo_surface.py` drives the whole arc inside the offline gate and fails if a step has no expectation, and the hosted GitHub Actions check runs that gate on every pull request and every push to main. The SAME script is still runnable headless and unattended as `make demo-selftest`, asserting every step and exiting non-zero when a claim stops being true. The hosted check also runs `make demo-selftest` and `make portability` as their own steps, so the headless arc and the portability tour fail a build rather than a meeting. `make demo-static` runs in no CI step, because the GitHub Actions workflow that named it was retired as unrunnable; documentation integrity keeps its own in-gate guard in `tests/unit/test_docs_links.py`. |
| **F3** Portability claim is executable `[all]` | PASS | `make portability` runs eight named checks with a pass or fail each: port map completeness, adapter construction and Protocol conformance, the offline family ANSWERING, the exit family REFUSING, in-place rewrite detection, anchored truncation detection with its control case, the JSONL export and foreign reload, and the no-cloud-SDK check. It prints what it does NOT prove and exits non-zero on any failure. |
| **G1** Declared doc authority order, kept true `[all]` | PASS | `AGENTS.md` declares SPEC > ARCHITECTURE > COMPLIANCE > README. |
| **G2** Compliance mapping table + adopter-owned crosswalk `[all]` | PASS | `COMPLIANCE.md` maps the FULL principle set (P-01 to P-13) and rule set (R1 to R8) to a control and an evidence file, with an explicit `TODO (repo owner)` row naming what must be added wherever the control is per-repo, so a rendered repo never silently claims coverage it lacks. The regulatory crosswalk is marked adopter-owned. |
| **G3** Documented, mechanised fork path `[all]` | PASS | `docs/ADOPTING.md` carries the kernel-vs-vertical boundary, the core-vs-adopter-owned file list, the human-decision checklist (scoring policy, control taxonomy, region, IdP, evidence sources, eval golden set) and the adoption checklist. `scripts/rename_fork.py` is the one-pass rename (package and console script, `TPRM_DDQ_` env prefix including the bare token in `render.tf.json`, the Terraform `name_prefix` stem, the distribution id), dry-run by default and validated against the same `name_prefix` regex Terraform enforces. |
| **G4** Retired `[all]` | N/A (retired) | Retired practice. Releases are tracked by git tag and the `pyproject.toml` version. |
| **G5** Role-specific FAQs referencing sibling systems `[all]` | PASS | `docs/faq/` carries the five role pages plus an index, each naming the owning catalog id for every adjacent capability (`compliance-advisory` for the outsourcing-rule text, `contract-obligation-extraction` for the contract register, `operational-resilience-mapping` for the downstream register reporting, `agent-guardrail-gateway`/2/3/4/5/7 for the horizontals) rather than re-documenting it. `features-faq.md` holds the full owns-vs-integrates map. |
| **G6** Contribution docs cover full extension touch list `[all]` | PASS | `CONTRIBUTING.md` carries file-by-file walkthroughs for BOTH "adding an adapter" and "adding a port or sub-service", each row naming the test that enforces it; the five-way drift guard in `tests/contract/test_port_parity.py` is what makes the list enforceable rather than advisory. |
| **G7** Markdown discipline: minimise em-dashes, validate mermaid `[all]` | PASS | Enforced, not merely observed: `make docs-check` (`scripts/check_docs_links.py`) fails on an em-dash or en-dash in any shipped `.md` or `.html`, on an unclosed code fence, and on a relative link that resolves to nothing. `tests/unit/test_docs_links.py` runs the same functions in the offline gate, with a control case proving the checker can go red. |

**Verdict counts:** 36 PASS, 1 PARTIAL, 1 GAP, 3 N/A. Every load-bearing check
(A1-A6, C1-C5, D1-D3, E1) is a full PASS, which is the point of the template: a new repo owes
the matrix its verticals, not its foundations.

D5, G3 and G5 are not gaps: the Terraform stack is written and
tested against a mocked provider, `docs/ADOPTING.md` plus `scripts/rename_fork.py` are the
mechanised fork path, and `docs/faq/` carries the five role pages. `docs/model-card.md` records
the model boundary, which in this repo is a `GenerationPort` seam with no model behind it: the
offline adapter is a deterministic rule-based stub and the managed one raises.

The one remaining GAP is genuinely per-repo rather than something the template could have filled
in:

- **B4** lift the policy numbers out of module constants and dataclass defaults into a `policy:`
  block in `config/settings.yaml` with a `from_policy(...)` constructor, so a bank sets its own
  scoring weights, financial bands, escalation floor, PII jurisdictions and eval thresholds as
  configuration rather than by editing code. `ScoringPolicy`, `FinancialPolicy` and
  `MemoReviewPolicy` are already frozen dataclasses the engines take as parameters, so this is a
  settings-loading change rather than an engine change.
